# Required for the API and reviewer dashboard
ADMIN_TOKEN=replace-with-a-long-random-secret
APP_API_KEY=

# Directory containing private model files
MODEL_DIR=./models

# Optional detector model. Leave empty to use classifier fallback.
DETECTOR_CHECKPOINT=

# Comma-separated browser/mobile origins
CORS_ORIGINS=http://localhost:3000
